#include "../../template.h"

int main(){

    int n=uscan();
    int i,a[n];
    f(i,n)a[i]=uscan();
    int cur_siz=1;
    while(cur_siz<n){
        ll l=,r=;
        ll temp[2*n];
        f(i,xxx){
            int a[cur_siz],b[cur_siz];
            f(j,cur_siz){
                
            }
        }
        f(i,min(2*n,)){
            a[]=temp[i];
        }
        cur_siz*=2;
    }

    f(i,n){
        cout<<a[i]<<' ';
    }pc('\n');
}